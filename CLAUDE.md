# craft-ai

## Git

Do not commit code to git unless explicitly instructed to do so. Even when work appears complete and tests pass, wait for an explicit instruction (e.g. "commit", "make a commit", "commit and push") before running `git commit`. Staging changes with `git add` is also off-limits without instruction.
