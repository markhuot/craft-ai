{% import '_includes/forms.twig' as forms %}

{{ forms.hidden({
    name: 'id',
    value: backend.id|default()
}) }}

{{ forms.textField({
    first: true,
    label: 'Name',
    name: 'name',
    value: old('name', backend),
    errors: [flash('error.name')]|filter,
}) }}

{% include backend.settingsView %}
